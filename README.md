
# âš½ Soccer Live Score API (DevOps Project)

A simple FastAPI backend that fetches soccer data from [Football-Data.org](https://www.football-data.org/).

## íº€ Run Locally
```bash
python -m venv .venv
source .venv/Scripts/activate   # Git Bash on Windows
pip install -r requirements.txt
uvicorn app.main:app --reload

